# 🛋️ AI Room Topology Matcher (Умный поиск интерьеров для Staging)

**AI Room Topology Matcher** — это профессиональный скрипт на базе компьютерного зрения для подбора идеальных пустых комнат-референсов. 
Проект создан специально для задач AI-стейджинга (виртуальной расстановки мебели), где критически важно 100% совпадение перспективы, угла обзора и расположения света.

## ⚠️ Какую проблему решает проект?
Стандартные нейросети для поиска похожих картинок (ResNet50) или оценки глубины (Depth Anything / MiDaS) часто ошибаются в пустых помещениях:
1. **Зеркальный эффект:** Они путают левый угол с правым.
2. **Иллюзия перспективы:** Принимают плоскую фронтальную стену с падающей тенью за уходящий вдаль коридор.
3. **Брак при наложении (Коллизии):** Они подбирают маску мебели, которая при наложении на новую комнату перекрывает окна или дверные проемы.

## 🧠 Наше решение: Heuristic Topology + Staging Control
Мы отказались от "слепого" сравнения пикселей и перевели задачу в плоскость жесткой архитектурной геометрии. 
Алгоритм использует нейросеть (SegFormer) только как "глаза" для разметки пола и стен, а все решения принимает детерминированная математика.

### Как работает алгоритм (Pipeline):

1. **Анализ Топологии (Каркас комнаты):**
   По линии плинтуса алгоритм математически вычисляет координату "Дальнего угла". Благодаря этому скрипт жестко классифицирует:
   - *Тип комнаты:* Фронтальная (есть прямая задняя стена) или Угловая.
   - *Доминирующая стена:* Левая или Правая. Зеркальные комнаты мгновенно отсекаются.
   
2. **Анализ Освещения:**
   Вычисляется, в какой трети кадра находятся окна (слева, по центру, справа).

3. **Виртуальная примерка (Collision Detection):**
   На этапе создания базы скрипт анализирует не только пустую комнату (`before`), но и готовый дизайн-проект (`_after`). Он вырезает "Отпечаток мебели" (силуэты шкафов, диванов, кроватей). 
   При поиске скрипт виртуально накладывает эту мебель на вашу пустую комнату. Если чужой шкаф перекрывает ваше окно — вариант жестко штрафуется.

## 🚀 Как запустить

Проект использует современный менеджер пакетов `uv`. Все зависимости прописаны прямо внутри скрипта `image_retrieval.py` (PEP 723), поэтому виртуальное окружение создается автоматически.

**1. Индексация базы:**
Положите фотографии пустых комнат и их готовые дизайны (с припиской `_after` в названии) в папку `database`.
```bash
uv run image_retrieval.py index database
```

**2. Поиск похожей комнаты (CLI):**
Для поиска идеальной пустой комнаты под ваш новый исходник:
```bash
uv run image_retrieval.py search путь/к/вашей_картинке.jpg --top-k 5
```

**3. Запуск FastAPI Микросервиса (API для сайта):**
Для интеграции с веб-сайтом предусмотрен готовый микросервис.
```bash
uv pip install -r retrieval_api/requirements.txt
uvicorn retrieval_api.main:app --reload --port 8000
```

Сервер поднимется на `http://localhost:8000`. 
Доступен единственный POST эндпоинт `/search`, который принимает файл картинки (`image`) и параметр `top_k`.

**Пример запроса через cURL:**
```bash
curl -X POST "http://localhost:8000/search?top_k=3" \
  -H "accept: application/json" \
  -H "Content-Type: multipart/form-data" \
  -F "image=@test_room.jpg"
```
В ответ придет JSON с отранжированным списком лучших комнат, их скорингом (включая штрафы за мебель) и base64-строкой готового дизайна (`_after`).